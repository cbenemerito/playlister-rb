<h1><%= name %> - <%= songs.count %> Songs</h1>

<ul>
	<% songs.each do |song| %>
		<li><a href="<%= "../songs/#{song.name}.html" %>"><%= song.name %></a> - <a href="<%= "../genres/#{song.genre.name}.html" %>"><%= song.genre.name %></a></li>
	<% end %>
</ul>